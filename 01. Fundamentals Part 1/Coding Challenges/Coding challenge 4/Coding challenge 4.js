// DATA 1 OPTION 1

const firstBill = 275;

const firstTip = firstBill >= 50 && firstBill <= 300 ? 0.15 * firstBill : 0.2 * firstBill;

console.log(`The bill was ${firstBill}, the tip was ${firstTip}, and the total value ${firstBill + firstTip}`);


// DATA 1 OPTION 2

const secondBill = 40;

const secondTip = secondBill >= 50 && secondBill <= 300 ? 0.15 * secondBill : 0.2 * secondBill;

console.log(`The bill was ${secondBill}, the tip was ${secondTip}, and the total value ${secondBill + secondTip}`);


// DATA 1 OPTION 3

const thirdBill = 430;

const thirdTip = thirdBill >= 50 && thirdBill <= 300 ?  0.15 * thirdBill : 0.2 * thirdBill;

console.log(`The bill was ${thirdBill}, the tip was ${thirdTip}, and the total value ${thirdBill + thirdTip}`);